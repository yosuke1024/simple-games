/**
 * Board constants. See docs/NUMBER_MATCH_RULES.md — the single source of truth
 * for the game rules these values belong to.
 */
export const COLS = 9;
export const INITIAL_CELLS = 27;
export const MAX_CELLS = 270;
/** Practically unlimited undo; normal games never come close to this. */
export const UNDO_HISTORY_LIMIT = 2000;
