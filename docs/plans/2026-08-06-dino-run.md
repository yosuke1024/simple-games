# Dino Run — 1 本追加の記録 (2026-08-06)

収録を 12 本から 13 本へ広げる。ブラウザのオフライン画面で誰もが一度は遊んだことの
ある「走って避けるだけ」のランナーを、この収録の流儀で作り直したもの。生成も記録も
端末内で完結し、コンテンツサーバーを必要としない([CONTRIBUTING.md](../../CONTRIBUTING.md)
の収録条件)。

仕様の正本は [DINO_RUN_RULES.md](../DINO_RUN_RULES.md)。ここは「なぜそう決めたか」だけを
残す。

## 決定事項(ユーザー承認済み)

| 項目   | 決定                                                                  |
| ------ | --------------------------------------------------------------------- |
| 収録   | ランナー **1 本**                                                     |
| id     | `dino-run`(i18n 接頭辞 `dino` / 保存接頭辞 `dr.`)                    |
| 主人公 | 恐竜                                                                  |
| 操作   | **ジャンプのみ**(しゃがみは試作後に取り下げ — 下記)                  |
| 進行   | エンドレス(レベルなし・デイリーなし)。記録はスコア = 走った距離      |

## 名前と見た目

`Dino Run` はジャンル名に近い一般名で、`Water Sort` `Sliding Puzzle` `Block Puzzle` と
同じ「ジャンルそのものを名前にする」流儀に揃う([BRAND.md](../BRAND.md): タイトルは
英語の固有名詞・全言語で同一表記)。

**見た目はジャンルの作法に合わせ、絵は自分で描いた。** 盤面比(600×150)・
物理・スコア表示の作法は、ブラウザのオフライン画面から続くこのジャンルの形に
合わせている — そこを外すと「それらしくない」ものにしかならない、というのが
1 度目と 2 度目の試作で分かったことである。一方で走者・サボテン・鳥・数字は
すべてこの収録のために描いたドット絵で、他社のスプライトを取り込んではいない。
色はシリーズのアクセントトークンに従う。

| ゲーム   | アクセント | ライト    | ダーク    | 選んだ理由                                       |
| -------- | ---------- | --------- | --------- | ------------------------------------------------ |
| Dino Run | 砂漠の草   | `#6e7a34` | `#b6c274` | Solitaire の緑と Brick Breaker の黄土の間の空き |

障害物をアクセント、走者と地面をインクで描く。**色が形より先に「これは邪魔物だ」と
言う**ので、鳥かサボテンかを読む前に避ける判断が始められる。

## 設計で迷い、決めたところ

**1. しゃがみを落とした。** 最初は「ジャンプ + しゃがみ」で作った。ボタンを 2 つ
並べる形まで作って試遊した結果、**入力が 1 つのほうがこの遊びは良い**という判断に
なった(ユーザー判断)。しゃがみが無くなると鳥も「跳ぶ / 跳ばない」の問いに揃うので、
頭の高さの鳥は落とし、低い鳥(跳ぶ)と高い鳥(跳ばない)の 2 つにした。
残った入力はプレイ領域のタップ・画面幅いっぱいの「↑」ボタン・Space/↑/W キー。

**2. 間隔を秒で決める。** 速度は 200px/s から 460px/s まで上がる。間隔をピクセルで
決めると、開始時に公平だった隙間が終盤には物理的に飛び越せない幅になる。
**間隔は秒で引き、最短 0.78 秒**(ジャンプの滞空 0.61 秒より長い)とした。
難易度を上げるのは速度だけで、密度は最後まで増やさない。

**3. 高い鳥を「何もしない」問いにした。** 立って走る限り当たらない鳥を 1 種類だけ
置いた。反射でジャンプすると当たる、この遊びで唯一「押さない」が正解の形である。
その代わり**高い鳥の後ろには 1.1 秒を空ける** — 頭上にいる間に次の障害物のために
飛ばされるなら、それは避けられない死になるため。

**4. ライフも復活も作らない。** 1 度触れたら終わり。エンドレスでリトライが即時・
無料である以上、失敗は安い。「もう少し生き延びる仕組み」は、あとから売り物にできる
形をしている — この製品はその形を持たない([PRODUCT_PRINCIPLES.md](../PRODUCT_PRINCIPLES.md))。

**5. 保存しない。** リアルタイムのコースを復元しても「開いた瞬間にサボテンが目の前に
ある」ものにしかならない。アーケード 2 本(Brick Breaker / Sky Fighter)と同じ判断で、
保存キーは `dr.stats` と `dr.flags` の 2 つだけ。

**6. レビュー依頼の数え方。** 走りは数秒で終わることがある。全部を「1 ゲーム完了」と
数えると、10 秒遊んだ人にストア評価を尋ねることになる。**スコア 100 以上のランだけ**
`recordGameCompleted()` を呼ぶ([REVIEW_PROMPT_POLICY.md](../REVIEW_PROMPT_POLICY.md))。

**7. スプライトは絵として書く。** 最初の版は矩形の座標配列で走者を描き、数字としては
妥当なのに**恐竜に見えなかった**(頭が小さく胴が細い)。編集中に形が見えないことが
原因だったので、`ui/components/sprites.ts` を `#` / `.` / 目の穴 `o` のドット絵に
書き換えた。ソースが画面と同じものを見せるので、頭の大きさの間違いが差分で分かる。
走者は 20×21 マス(40×42px)で、大きな頭・短く太い首・開いた口・後ろへ伸びる尾の
4 つで恐竜になる。

**8. スコアはコースに描く。** アプリの文字で上部に出していたときは、どの画面とも
同じ「アプリの見出し」にしか見えなかった。5 桁ゼロ埋め + `HI` を走者と同じ矩形で
コースの右上に描いたら、一目でこのゲームだと分かるようになった。数字は翻訳が要らず、
フォントの読み込みも要らない。読み上げ用の文字列だけ DOM に残している。

## 作らなかったもの

- **昼夜の切り替え**: 画面の明暗はアプリ設定がすでに持っている。ゲームが勝手に
  反転させない。
- **しゃがみ**(§1 の判断)・**二段ジャンプ**: どんな間隔も飛び越せるようになり、
  コースが問いを出さなくなる。
- **デイリー**: 生成はシード決定的なので、必要になれば後から足せる。
- **オンラインランキング**: 追加の固定インフラ費を作らない。

## 検証

- ユニット: エンジン(開始 / ジャンプ / 生成間隔 / 衝突 / スコア)、
  ゴールデンなコース(`game/compatibility.test.ts`)、画面(Root / リーク)。
- 実機相当: ブラウザで実走(ライト / ダーク)。走る・跳ぶ・ぶつかる・
  結果画面までを目視で確認した。スプライトは 12 倍に拡大して 1 マスずつ確認した。
- サイズ Gate: `game-dino-run` は gzip 12.6KB(予算 500KB)。エントリは +1.0%。
